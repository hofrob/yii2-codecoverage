# global codeception file to run tests from all apps
include:
    - common
    - frontend
    - backend
paths:
    log: console/runtime/logs
settings:
    colors: true

coverage:
    enabled: true
    include:
        - common/models/*
        - frontend/controllers/*
        - frontend/models/*
